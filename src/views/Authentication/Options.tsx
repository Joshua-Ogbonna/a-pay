import React, {useState} from 'react'
import axios from 'axios'

interface IOptions {
    atId: null
    accountType: string
    status: string
}[]

const Options = () => {
  return (
    <div>Options</div>
  )
}

export default Options